import { Box, Text } from "@chakra-ui/react";
import React from "react";

export const DataSelector = () => {
  return (
    <Box h="full" p={2}>
      <Text fontWeight="bold">Choose datasets</Text>
      Explainit
    </Box>
  );
};
